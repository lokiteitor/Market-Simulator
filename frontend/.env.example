# Base URL de la API (detrás de Caddy). El WS deriva de esta misma base.
VITE_API_BASE_URL=http://localhost:9080/v1

# Factor de simulación de la corrida. DEBE coincidir con SIM_TIME_FACTOR del
# backend (mismo contrato que bots-v1/config.yaml) o los hints de tiempo mienten.
VITE_SIM_TIME_FACTOR=5
